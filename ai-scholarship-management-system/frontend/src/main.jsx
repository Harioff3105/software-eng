import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import axios from'axios';import'./style.css';
const API='http://localhost:5000/api';
function App(){const [items,setItems]=useState([]),[q,setQ]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[msg,setMsg]=useState('');const token=localStorage.getItem('token');
const search=async()=>{const r=await axios.get(`${API}/scholarships?q=${encodeURIComponent(q)}`);setItems(r.data)};useEffect(()=>{search()},[]);const login=async()=>{try{const r=await axios.post(`${API}/auth/login`,{email,password});localStorage.setItem('token',r.data.token);setMsg('Welcome back!')}catch(e){setMsg(e.response?.data?.message||'Login failed')}};const register=async()=>{try{const r=await axios.post(`${API}/auth/register`,{name,email,password});localStorage.setItem('token',r.data.token);setMsg('Account created!')}catch(e){setMsg(e.response?.data?.message||'Registration failed')}};const apply=async id=>{if(!token)return setMsg('Please login before applying.');await axios.post(`${API}/applications`,{scholarship:id},{headers:{Authorization:`Bearer ${token}`}});setMsg('Application submitted successfully.');};return <div><header><div className="brand">🎓 ScholarAI</div><span>Find scholarships matched to your goals</span></header><main><section className="hero"><h1>AI Scholarship Management System</h1><p>Discover opportunities, get intelligent recommendations, manage documents and track applications.</p><div className="search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search scholarship, provider or field..."/><button onClick={search}>Search</button></div></section><div className="grid"><section><h2>Scholarship Discovery</h2>{items.map(s=><article className="card" key={s._id}><div><h3>{s.title}</h3><p>{s.provider} · {s.description}</p><b>₹{s.amount||'Varies'}</b>{s.deadline&&<small>Deadline: {new Date(s.deadline).toLocaleDateString()}</small>}</div><button onClick={()=>apply(s._id)}>Apply</button></article>)}</section><aside className="panel"><h2>Account</h2><input placeholder="Name (for signup)" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={login}>Login</button><button className="secondary" onClick={register}>Create account</button><p>{msg}</p><hr/><h3>Four Modules</h3><ul><li>Scholarship Discovery & Search</li><li>AI Recommendation Engine</li><li>Document Management</li><li>Admin & Reporting</li></ul></aside></div></main></div>};createRoot(document.getElementById('root')).render(<App/>);
